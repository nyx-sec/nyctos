avoid-breaking-exported-api = false
msrv = "1.83"

# Workspace lint rules that clippy itself cannot express (e.g.
# `missing-instrument`) live in `../lints.toml` and are enforced by
# scripts under `.ci/`. See `lints.toml` for the current set.

